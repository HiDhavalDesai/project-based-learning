// Math = built-in object that provides a collection of properties and methods


// console.log(Math.PI);
// This will give you the numbers for pi.
// console.log(Math.E);
// This is Euler's Number, a mathematical constant, represented by the letter e, that is the base of the natural logarithm and is used in problems relating to exponential growth or decay.
// Used for the base of natural logarithms.    X = log(base(e))X

let a = 4;
let x = 3;      
let y = 2;
let z = 1;

// round a number, you can use the object.method -- object is Math, Method is round.
// z = Math.round(x);
// console.log(z);


// console.log(Math.sign(a))
// There is another method floor, which will also round down, a = 3.99, floor will give 3, to round up, you use ceil, 3.99, ceil will give you 4, 3.499 will also give you 4. trunc will eliminate any decimals. pow is used to raise a base to a power.
// console.log(Math.pow(x,y)) is how power is done, x is base, y is exponent. Natural log of 10 is 2.30 (console.log(Math.log(a)))



// Find the max of this list
let max = Math.max(z,x,y)
console.log(max);

min = Math.min(z,x,y);
console.log(min);


// Object.method
// Math.round - will always round down
// Math.floor - floor (down)
// Math.ceil - ceiling (up)
// Math.trunc - short for truncate, will eliminate any decimals
// Math.pow - raise a base to a power.  (console.log(Math.pow(x,y)))
// Math.sqrt - this is the square root.
// Math.log -   natural log of a function
// Math.abs - absolute value of a number. 
// Math.sign - gives you a negative 1 or positive 1 or 0; for negative is gives negative 1, for positive it gives positive 1 and for 0 it gives 0.
// Math.max - max number from list
// Math.min - min number from list


// Trig functions
// sin, cos, tan